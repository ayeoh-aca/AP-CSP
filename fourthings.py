def custom_msg():
    age = input("What's your age: ")
    name = input("What's your name: ")
    major = input("What's your major: ")
    print(f"Hello {name}. You're {age} years old in {major}.")
def firstletter(studentnames):
    for name in studentnames:
        student = studentnames[0]
        if student == 'A':
            student = studentwitha
    return studentwitha
firstletter(["Alan", "Sam", "Ethan"])
def avg_grade(grades):
    sum += grades
    amount += 1
    avg = sum/amount
    return avg
avg_grade([23, 24, 25, 26])